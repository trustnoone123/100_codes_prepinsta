// Inbuilt function
#include<stdio.h>
#include<math.h> 
int main() 
{
    int base = 24;
    int exp = 2;
    int result;
    result = pow(base, exp);
    printf("%d ^ %d = %d\n", base, exp, result);
    return 0;
}

